<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" type="text/css" href="../css/styles.css">
</head>
<body>
<h1><%= title %></h1>
<p>Welcome to <%= message %></p>
<ul>
    <% articles.forEach((article, index) => { %>
        <li>
            <a href="#article<%= index + 1 %>"><%= article.title %></a>
        </li>
    <% }) %>
</ul>

<!-- Тут будуть відображені статті -->
<% articles.forEach((article, index) => { %>
    <h2 id="article<%= index + 1 %>"><%= article.title %></h2>
    <p><%= article.content %></p>
    <hr>
<% }) %>
</body>
</html>
